version: "3"
services:
  web:
    image: nginx
    container_name: nginx
    ports:
      - 80:80
    volumes:
      - /etc/nginx/:/etc/nginx/
      - /etc/nginx/conf.d/:/etc/nginx/conf.d
  spring:
    build: .
    ports:
      - 8081:8081
    volumes:
      - ./:/root/
    environment:
      active: real1